# 배포 전 보안 체크리스트

**중요**: 프로덕션 배포 전 반드시 확인해야 할 보안 사항들입니다.

---

## 🔐 환경 변수 설정

### 1. 필수 환경 변수 확인

```bash
# Supabase Edge Functions 환경 변수 설정
supabase secrets set PASSWORD_PEPPER="$(openssl rand -hex 32)"
supabase secrets set ADMIN_API_TOKEN="$(uuidgen)"
supabase secrets set OPENAI_API_KEY="sk-your-actual-key"
```

### 2. 환경 변수 보안 확인
- [ ] PASSWORD_PEPPER는 최소 32자 이상의 무작위 문자열
- [ ] 모든 API 키가 환경 변수로 설정됨 (코드에 하드코딩 X)
- [ ] .env 파일이 .gitignore에 포함됨
- [ ] 프로덕션과 개발 환경의 키가 분리됨

---

## 🛡️ 서버 보안 확인

### 1. 보안 헤더
- [ ] X-Content-Type-Options: nosniff 설정됨
- [ ] X-Frame-Options: DENY 설정됨
- [ ] Strict-Transport-Security 설정됨
- [ ] Content-Security-Policy 설정됨

### 2. Rate Limiting
- [ ] 로그인 시도 제한 (1분 5회) 활성화
- [ ] 메시지/댓글 생성 제한 활성화
- [ ] 질문/답변 생성 제한 활성화

### 3. 세션 관리
- [ ] 세션 타임아웃 30분 설정됨
- [ ] 세션 자동 갱신 활성화
- [ ] 만료된 세션 자동 삭제 확인

---

## 👥 관리자 계정 보안

### 1. 초기 관리자 계정 생성
```bash
# 최고 관리자 계정은 반드시 강력한 비밀번호 사용
# 예: MyP@ssw0rd!2026#Admin (12자 이상, 4종 조합)
```

### 2. 비밀번호 정책 확인
- [ ] 최소 8자 이상
- [ ] 영문 대소문자, 숫자, 특수문자 중 3가지 이상
- [ ] bcrypt Round 12로 해싱
- [ ] Pepper 추가됨

### 3. 권한 관리
- [ ] 최고 관리자(Primary Admin)와 일반 관리자 구분
- [ ] 계정 관리는 최고 관리자만 가능
- [ ] 불필요한 계정 삭제

---

## 🌐 HTTPS 설정

### 1. SSL/TLS 인증서
- [ ] HTTPS 강제 적용 (Strict-Transport-Security)
- [ ] 유효한 SSL 인증서 설치
- [ ] HTTP → HTTPS 리다이렉트 설정

### 2. CORS 정책
- [ ] 허용된 Origin만 접근 가능
- [ ] Preflight 요청 처리 확인

---

## 📝 로깅 및 모니터링

### 1. 활동 로그
- [ ] 관리자 로그인 성공/실패 기록
- [ ] 데이터 변경 작업 기록
- [ ] 보안 이벤트 기록

### 2. 에러 로그
- [ ] 에러 메시지에 민감정보 미포함 확인
- [ ] 스택 트레이스 클라이언트 노출 방지

---

## 🔍 입력 검증

### 1. 서버 측 검증
- [ ] 모든 사용자 입력 Sanitization 적용
- [ ] XSS 방지 (HTML 태그 제거)
- [ ] 금지어 필터링 활성화
- [ ] 입력 길이 제한 적용

### 2. 프론트엔드 검증
- [ ] `/src/app/utils/security.ts` 함수 사용 확인
- [ ] 클라이언트 측 사전 검증 활성화

---

## 📦 의존성 보안

### 1. 패키지 업데이트
```bash
# 취약한 패키지 확인
pnpm audit

# 자동 수정 가능한 취약점 해결
pnpm audit --fix
```

### 2. 정기 점검
- [ ] 월 1회 의존성 업데이트 점검
- [ ] 보안 패치 즉시 적용
- [ ] 사용하지 않는 패키지 제거

---

## 🗄️ 데이터베이스 보안

### 1. 접근 제어
- [ ] Row Level Security (RLS) 정책 확인
- [ ] 최소 권한 원칙 적용
- [ ] 관리자 계정 외 직접 접근 차단

### 2. 백업
- [ ] 정기 백업 스케줄 설정
- [ ] 백업 데이터 암호화
- [ ] 복구 절차 테스트

---

## 🧪 보안 테스트

### 1. 수동 테스트
- [ ] XSS 공격 시도
- [ ] SQL Injection 시도
- [ ] CSRF 공격 시도
- [ ] 무차별 대입 공격 시도

### 2. 자동화 테스트
```bash
# OWASP ZAP 또는 Burp Suite 사용 권장
```

---

## 📋 최종 체크리스트

### 배포 전 필수 확인사항
- [ ] 모든 환경 변수 설정 완료
- [ ] 보안 헤더 7종 적용 확인
- [ ] Rate Limiting 동작 확인
- [ ] 세션 타임아웃 동작 확인
- [ ] HTTPS 강제 적용 확인
- [ ] 에러 메시지 민감정보 미포함 확인
- [ ] 관리자 활동 로그 기록 확인
- [ ] 입력 검증 동작 확인
- [ ] 금지어 필터 동작 확인
- [ ] 파일 업로드 제한 확인

### 배포 후 모니터링
- [ ] 첫 1주일 일일 로그 모니터링
- [ ] 비정상 접근 시도 감지
- [ ] 성능 모니터링 (Rate Limiting 영향)
- [ ] 사용자 피드백 수집

---

## 🚨 보안 사고 대응

### 긴급 연락망
1. 개발팀 담당자
2. 성남시청 정보통신과
3. Supabase 지원팀

### 사고 발생 시 조치
1. 즉시 해당 계정 비활성화
2. 세션 전체 무효화
3. 로그 분석
4. 비밀번호 강제 변경
5. 사고 보고서 작성

---

## 📞 지원

### 문서
- `SECURITY_COMPLIANCE.md` - 상세 보안 조치 사항
- `SECURITY_CHECKLIST.md` - 빠른 참조 가이드
- `.env.example` - 환경 변수 예시

### 문의
보안 관련 문의사항은 개발팀으로 연락해 주세요.

---

**버전**: 2.0.0  
**최종 업데이트**: 2026년 5월 17일
