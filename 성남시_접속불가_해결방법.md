# 성남시 개발 톡톡 - 접속 불가 긴급 해결 방법

## 📋 요약

**문제**: 성남시 내부 네트워크에서 `urel.figma.site` 접속 시 404 에러 발생  
**원인**: 공공기관 방화벽 정책 + SPA 라우팅 설정 문제  
**긴급 해결**: Hash Router로 변경 완료 (2026.04.23)  
**추가 필요 조치**: 성남시 IT 부서 방화벽 설정 변경

---

## ⚡ 즉시 확인 사항

### 1단계: 외부 네트워크에서 테스트
성남시 내부망이 **문제인지** 확인하기 위해:

```
✅ 개인 휴대폰 핫스팟으로 접속 시도
✅ 집에서 개인 인터넷으로 접속 시도
✅ 카페/공공 와이파이로 접속 시도
```

**외부에서 정상 접속되면** → 성남시 방화벽 문제 확정

### 2단계: 새 URL로 접속
기존 북마크/즐겨찾기를 **삭제**하고 새로운 URL로 접속:

```
❌ 기존: https://urel.figma.site/dashboard
✅ 신규: https://urel.figma.site/#/dashboard

❌ 기존: https://urel.figma.site/community
✅ 신규: https://urel.figma.site/#/community
```

**차이점**: URL에 `#` (해시) 기호가 추가되었습니다.

### 3단계: 브라우저 캐시 완전 삭제

**Chrome 사용자**:
1. `Ctrl + Shift + Delete` 키 누르기
2. "전체 기간" 선택
3. "캐시된 이미지 및 파일" 체크
4. "데이터 삭제" 클릭
5. 브라우저 재시작

**Edge 사용자**:
1. `Ctrl + Shift + Delete` 키 누르기
2. "전체 기간" 선택
3. "캐시된 이미지 및 파일" 체크
4. "지금 지우기" 클릭
5. 브라우저 재시작

---

## 🔥 성남시 IT 부서 긴급 요청 사항

### 방화벽 화이트리스트 등록 필요

다음 도메인들을 **즉시 허용**해주셔야 합니다:

```
1. urel.figma.site (메인 사이트)
2. *.figma.site (Figma 관련 모든 서브도메인)
3. *.supabase.co (백엔드 서버)
4. fonts.googleapis.com (폰트 로딩)
```

### 확인 방법 (IT 담당자용)

**Windows 명령 프롬프트**에서 실행:
```cmd
ping urel.figma.site
nslookup urel.figma.site
tracert urel.figma.site
```

**정상인 경우**:
```
응답: 시간=20ms TTL=54
```

**차단된 경우**:
```
요청 시간이 만료되었습니다.
또는
연결할 수 없습니다.
```

---

## 🛠️ 기술 담당자 참고 사항

### 변경 내역
- **이전**: `createBrowserRouter` (서버 리라이트 필요)
- **현재**: `createHashRouter` (서버 설정 불필요)

### Hash Router란?
- URL에 `#` 기호 사용 (예: `/#/dashboard`)
- 서버에서 모든 요청을 `index.html`로 보낼 필요 없음
- 404 에러 근본적으로 해결
- **단점**: URL이 약간 길어짐 (기능상 문제 없음)

### 배포 환경
- **호스팅**: Figma Make (정적 사이트 호스팅)
- **백엔드**: Supabase Edge Functions
- **데이터베이스**: Supabase PostgreSQL
- **프론트엔드**: React 18 + TypeScript + Vite

---

## 📞 단계별 문제 해결 가이드

### 상황 1: "사이트에 연결할 수 없음" 에러

**원인**: DNS 또는 방화벽 차단  
**해결**:
1. IT 부서에 `urel.figma.site` 화이트리스트 등록 요청
2. 외부 네트워크로 접속 확인
3. VPN 사용 중이면 VPN 끄고 재시도

### 상황 2: "404 Not Found" 에러

**원인**: 구버전 캐시 또는 잘못된 URL  
**해결**:
1. URL에 `#` 포함되어 있는지 확인
2. 브라우저 캐시 완전 삭제
3. 새 탭에서 `https://urel.figma.site` 직접 입력

### 상황 3: "페이지가 느리거나 멈춤"

**원인**: JavaScript 차단 또는 오래된 브라우저  
**해결**:
1. JavaScript 활성화 확인
2. Chrome 90+ 또는 Edge 90+ 사용
3. 보안 프로그램(V3, 알약 등) 예외 설정

### 상황 4: "일부 기능만 작동 안 함"

**원인**: Supabase 백엔드 접속 차단  
**해결**:
1. IT 부서에 `*.supabase.co` 화이트리스트 등록 요청
2. F12 → Console 탭에서 에러 메시지 확인
3. 에러 메시지 스크린샷 전달

---

## 📊 체크리스트 (성남시 담당자용)

### IT 부서 확인 사항
- [ ] 방화벽에서 `urel.figma.site` 허용
- [ ] 방화벽에서 `*.figma.site` 허용
- [ ] 방화벽에서 `*.supabase.co` 허용
- [ ] 프록시 서버 예외 설정
- [ ] HTTPS 트래픽 허용
- [ ] WebSocket 연결 허용

### 사용자 확인 사항
- [ ] 최신 브라우저 사용 (Chrome/Edge 90+)
- [ ] JavaScript 활성화
- [ ] 쿠키 허용
- [ ] 팝업 차단 해제 (주소 검색용)
- [ ] 브라우저 캐시 삭제
- [ ] 새 URL (`/#/` 포함) 사용

---

## 🎯 최종 권장 사항

### 단기 (즉시):
1. ✅ **Hash Router 적용 완료** (2026.04.23)
2. ⏳ IT 부서 방화벽 설정 변경 요청
3. ⏳ 사용자 안내: 브라우저 캐시 삭제

### 중기 (1-2주):
1. 성남시 전용 도메인 연결 검토 (예: dev.seongnam.go.kr)
2. Apache + PHP 환경 이관 계획 수립
3. 내부망 전용 서버 구축 검토

### 장기 (1개월+):
1. 성남시 데이터센터에 독립 서버 구축
2. Apache + PHP + MySQL 완전 이관
3. 외부 의존성 제거 (Figma, Supabase → 자체 서버)

---

## 🆘 긴급 연락처

**접속 불가 시 연락처**:
- 개발팀: [전화번호]
- IT 지원: [성남시 IT 부서 연락처]
- 배포 관련: Figma Make 고객센터

**에러 리포트 시 첨부 자료**:
1. 에러 메시지 스크린샷 (F12 → Console)
2. 접속 시도한 URL
3. 브라우저 종류 및 버전
4. 네트워크 환경 (내부망/외부망)

---

**작성일**: 2026년 4월 23일 오후  
**긴급도**: 🔴 높음  
**조치 완료**: Hash Router 변경  
**추가 필요**: 성남시 방화벽 설정  
